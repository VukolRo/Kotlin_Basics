fun main() {
    val a = readln().first()
    val b = readln().first()
    val c = readln().first()
    if (a + 1 == b && b + 1 == c) {
        println(true)
    } else {
        println(false)
    }
}
